# .NET 5 samples

## Local development

Build the solution:

```bash
dotnet build
```

Run the samples:

```bash
# runs source generator sample console
dotnet run --project samples/SourceGeneratorSampleConsole
```
